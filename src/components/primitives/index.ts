// Shared UI primitives. New screens build from these instead of re-implementing the button, card,
// row, section heading and stat tile per file (the copy-paste that the CEO audit flagged as the
// app's largest maintenance liability). Import from "@/components/primitives".

export { BackButton, type BackButtonProps } from "./back-button"
export { Button, type ButtonProps } from "./button"
export { Card, type CardProps } from "./card"
export { Row, type RowProps } from "./row"
export { Section, type SectionProps } from "./section"
export { StatTile, type StatTileProps } from "./stat-tile"
